# Shared Components

This directory contains components shared across the application.

## Structure

- `app-shell.tsx` - Main application shell with sidebar and layout
- `providers.tsx` - React Query provider setup
- `theme-provider.tsx` - Theme provider for dark/light mode
- `user-menu.tsx` - User menu component

## Usage

These components are used throughout the application to provide consistent layout and functionality.

